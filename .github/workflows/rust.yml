name: Rust

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Build
      run: curl https://raw.githubusercontent.com/Electrics-Eagles/PiElectricsEagles/master/dev/00_build_script.sh | sh

